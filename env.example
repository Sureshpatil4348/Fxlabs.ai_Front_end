# MT5 React Frontend Environment Configuration

# WebSocket URL for Market v2 (probe mode)
# Default: ws://localhost:8000/market-v2
REACT_APP_WEBSOCKET_URL=ws://localhost:8000/market-v2

# For production or remote server, change to:
# REACT_APP_WEBSOCKET_URL=ws://your-server-ip:8000/market-v2
# REACT_APP_WEBSOCKET_URL=ws://192.168.1.100:8000/market-v2
# REACT_APP_WEBSOCKET_URL=wss://your-domain.com:8000/market-v2

# Other possible configurations:
# REST API Base (optional; defaults to https://api.fxlabsprime.com)
# REACT_APP_API_BASE_URL=http://localhost:8000
# Optional API key for OHLC endpoint (sent as X-API-Key header)
# REACT_APP_API_TOKEN=your_api_key_here
# REACT_APP_DEBUG=true

# OHLC debug logging (default: true)
REACT_APP_ENABLE_OHLC_DEBUG_LOGS=true
# Select symbol/timeframe to log (e.g., BTCUSDm + H4)
REACT_APP_OHLC_DEBUG_SYMBOL=BTCUSDm
REACT_APP_OHLC_DEBUG_TF=H4

# IP Info integration (client uses Netlify Function proxy)
# Endpoint for browser to call (do not change unless you move the function)
REACT_APP_IP_INFO_FUNCTION_URL=/.netlify/functions/ip-info

# Netlify Function server-side secrets (DO NOT prefix with REACT_APP_)
# Configure these in Netlify environment variables for your site
# For local Netlify dev, place them in a local .env file at repo root
# API-specific naming for IP Info endpoint
ASOASIS_API_IP_INFO_CLIENT_ID=your-client-id
ASOASIS_API_IP_INFO_CLIENT_SECRET=your-client-secret

# Advanced TradingView widget (feature flag)
# OFF by default; set to true to enable advanced widget with state retention and custom indicators
REACT_APP_FEATURE_FLAG_USE_ADVANCED_TRADINGVIEW_WIDGET=false

# Subscription check bypass (development/testing only)
# Set to 'true' to skip all subscription status checks and allow unlimited access
# WARNING: Only use in development/testing environments, never in production
REACT_APP_BYPASS_SUBSCRIPTION_CHECK=false

# Stripe Subscription Management
# Supabase Edge Function URL for creating Stripe checkout sessions
REACT_APP_SUPABASE_STRIPE_CREATE_CHECKOUT_FUNCTION_URL=https://your-supabase-url/functions/v1/stripe-create-checkout-session
# Supabase Edge Function URL for cancelling Stripe subscriptions
REACT_APP_CANCEL_SUBSCRIPTION_FUNCTION_URL=https://your-supabase-url/functions/v1/cancel-subscription
# Supabase Anon Key (used for authentication with Supabase Edge Functions)
REACT_APP_SUPABASE_ANON_KEY=your-supabase-anon-key
# Enable embedded Stripe (set to 'true' for embedded checkout, 'false' for link)
REACT_APP_ENABLE_EMBEDDED_STRIPE=false
